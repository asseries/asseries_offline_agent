name: asseries_offline_agent
description: Offline AI coding agent powered by Ollama — Aida Flutter Edition
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  flutter_markdown: ^0.7.4
  highlight: ^0.7.0
  flutter_highlight: ^0.7.0
  uuid: ^4.5.1
  path: ^1.9.0
  file_picker: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
